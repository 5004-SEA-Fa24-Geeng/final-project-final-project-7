package model.team;

import gameEnum.Teams;

public class ComTeam extends Team{
    /**
     * Constructor for ComTeam
     * @param teamName enum in Teams
     */
    public ComTeam(Teams teamName) {
        super(teamName);
    }
}
