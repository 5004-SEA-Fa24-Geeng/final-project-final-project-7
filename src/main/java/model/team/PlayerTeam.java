package model.team;

import gameEnum.Teams;

public class PlayerTeam extends Team{
    /**
     * Constructor for PlayerTeam
     * @param teamName enum in Teams
     */
    public PlayerTeam(Teams teamName) {
        super(teamName);
    }
}
